// Sentinel form value for "across the gym" (no section attribution). Kept
// out of the "use server" module so it can be imported from client forms
// without dragging a server action into the bundle.
export const GYM_WIDE_VALUE = "__gym_wide__";
